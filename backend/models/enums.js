const channelType = Object.freeze({
    TEXT: 'TEXT',
    AUDIO: 'AUDIO',
    VIDEO: 'VIDEO'
});

// ... rest of enums

const memberRole = Object.freeze({
    ADMIN: 'ADMIN',
    MEMBER: 'MEMBER'
});

module.exports = { channelType, memberRole };